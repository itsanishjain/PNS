### Run locally

#### Run nextjs app locally

`yarn install`

`yarn run dev`


#### Run smart contract

`cd smart-contract`

`yarn install`

> In one terminal

`npx hardhat node` 

> In another terminal

`npx hardhat run scripts/deploy.js --network localhost`

